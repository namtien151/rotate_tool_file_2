name: roate_text_tool2
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  flutter_svg: ^2.0.10+1
  file_picker: ^8.1.2
  flutter_animate: ^4.5.0
  syncfusion_flutter_pdfviewer: ^27.1.50
  http: ^1.2.2
  process_run: ^1.2.1
  ffi: ^2.0.1
  win32: 5.5.4
dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

  assets:
    - assets/image/
    - assets/svg/